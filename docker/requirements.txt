# ============================================================
#  容器镜像所需的 Python 依赖
#
#  Flask / requests 与各应用目录内的 requirements.txt 保持一致
#  （7 个应用的 requirements.txt 完全相同）。
#  注意：若以后某个应用新增了依赖，请同步补充到这里，否则容器内会 ImportError。
# ============================================================
Flask>=3.0
requests>=2.31